---
sidebar_position: 5
title: POST - Create Template Folder
hide_table_of_contents: true
---

import React, { useState } from 'react';

export const ChevronDown = ({ size = 16, className = "" }) => (
  <svg
    xmlns="http://www.w3.org/2000/svg"
    width={size}
    height={size}
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    strokeWidth="2"
    strokeLinecap="round"
    strokeLinejoin="round"
    style={{ transition: 'transform 0.3s' }}
    className={className}
  >
    <path d="m6 9 6 6 6-6"/>
  </svg>
);

export const Sparkles = ({ size = 16, className = "" }) => (
  <svg
    xmlns="http://www.w3.org/2000/svg"
    width={size}
    height={size}
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    strokeWidth="2"
    strokeLinecap="round"
    strokeLinejoin="round"
    className={className}
  >
    <path d="m12 3 1.912 5.813a2 2 0 0 0 1.275 1.275L21 12l-5.813 1.912a2 2 0 0 0-1.275 1.275L12 21l-1.912-5.813a2 2 0 0 0-1.275-1.275L3 12l5.813-1.912a2 2 0 0 0 1.275-1.275L12 3Z"/>
    <path d="M5 3v4"/>
    <path d="M19 17v4"/>
    <path d="M3 5h4"/>
    <path d="M17 19h4"/>
  </svg>
);

export const CopyIcon = ({ size = 14, className = "" }) => (
  <svg xmlns="http://www.w3.org/2000/svg" width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
);

export const CheckIcon = ({ size = 14, className = "" }) => (
  <svg xmlns="http://www.w3.org/2000/svg" width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className={className}><polyline points="20 6 9 17 4 12"/></svg>
);

export const HighlightedCode = ({ code }) => {
  const highlight = (text) => {
    let highlighted = text
      .replace(/&/g, '&amp;')
      .replace(/</g, '&lt;')
      .replace(/>/g, '&gt;')
      .replace(/(\/\/.*$)/gm, '<span style="color: #8b949e">$1</span>')
      .replace(/(#.*$)/gm, '<span style="color: #8b949e">$1</span>')
      .replace(/("(?:[^"\\]|\\.)*")|('(?:[^'\\]|\\.)*')/g, '<span style="color: #a5d6ff">$1</span>')
      .replace(/\b(import|from|const|await|new|var|require|return|if|else|public|private|class|interface|package|use|namespace|def|func|true|false|null)\b/g, '<span style="color: #ff7b72">$1</span>')
      .replace(/\b(TechWriter|TechWriterClient|RequestOptions|TemplateFolder|TemplateFolderParams|context|ctx|options|client|tw|sc|service)\b/g, '<span style="color: #ffa657">$1</span>')
      .replace(/\b([a-z_][a-z0-9_]*)(?=\s*\()/gi, '<span style="color: #d2a8ff">$1</span>')
      .replace(/\b(\d+)\b/g, '<span style="color: #79c0ff">$1</span>');

    return <code dangerouslySetInnerHTML={{ __html: highlighted }} />;
  };

  return (
    <pre style={{ margin: 0, background: 'transparent', whiteSpace: 'pre-wrap', fontFamily: 'monospace' }}>
      {highlight(code)}
    </pre>
  );
};

export const PostCreateTemplateFolderPage = () => {
  const [language, setLanguage] = useState('cURL');
  const [isOpen, setIsOpen] = useState(false);
  const [activeStatus, setActiveStatus] = useState('201');
  const [feedback, setFeedback] = useState(null);
  const [copiedSnippets, setCopiedSnippets] = useState({});

  const languages = [
    { id: 'curl', name: 'cURL' },
    { id: 'node', name: 'Node.js' },
    { id: 'python', name: 'Python' },
    { id: 'ruby', name: 'Ruby' },
    { id: 'php', name: 'PHP' },
    { id: 'java', name: 'Java' },
    { id: 'go', name: 'Go' },
    { id: 'dotnet', name: '.NET' }
  ];

  const requestSnippets = {
    'cURL': `curl -X POST "{{env_base_uri}}/api/public/template-folders" \\
  -H "Authorization: Bearer {YOUR_BEARER_TOKEN}" \\
  -H "Accept: */*" \\
  -H "Content-Type: application/json" \\
  -d '{
    "name": "Onboarding Templates",
    "groupIds": ["b81eceff-d8db-4e10-a10d-01010f294fe9"],
    "userIds": [
      "08bc7305-5d85-424b-b6ab-0007d35d501f",
      "1a66d10c-dcba-4d86-8bf0-000c5b7086c2"
    ]
  }'`,
    'Node.js': `import TechWriter from 'techwriter';
const tw = new TechWriter('tw_test_51H59...');

const folder = await tw.templateFolders.create({
  name: 'Onboarding Templates',
  groupIds: ['b81eceff-d8db-4e10-a10d-01010f294fe9'],
  userIds: [
    '08bc7305-5d85-424b-b6ab-0007d35d501f',
    '1a66d10c-dcba-4d86-8bf0-000c5b7086c2'
  ]
});`,
    'Python': `from techwriter import TechWriterClient
client = TechWriterClient("tw_test_51H59...")

folder = client.v1.template_folders.create(
  name="Onboarding Templates",
  group_ids=["b81eceff-d8db-4e10-a10d-01010f294fe9"],
  user_ids=[
    "08bc7305-5d85-424b-b6ab-0007d35d501f",
    "1a66d10c-dcba-4d86-8bf0-000c5b7086c2"
  ]
)`,
    'Ruby': `require 'techwriter'
client = TechWriter::Client.new("tw_test_51H59...")

folder = client.v1.template_folders.create(
  name: 'Onboarding Templates',
  group_ids: ['b81eceff-d8db-4e10-a10d-01010f294fe9'],
  user_ids: [
    '08bc7305-5d85-424b-b6ab-0007d35d501f',
    '1a66d10c-dcba-4d86-8bf0-000c5b7086c2'
  ]
)`,
    'PHP': `$tw = new \\TechWriter\\Client("tw_test_51H59...");

$folder = $tw->templateFolders->create([
  'name' => 'Onboarding Templates',
  'groupIds' => ['b81eceff-d8db-4e10-a10d-01010f294fe9'],
  'userIds' => [
    '08bc7305-5d85-424b-b6ab-0007d35d501f',
    '1a66d10c-dcba-4d86-8bf0-000c5b7086c2'
  ]
]);`,
    'Java': `TechWriterClient client = new TechWriterClient("tw_test_51H59...");

TemplateFolderParams params = TemplateFolderParams.builder()
  .setName("Onboarding Templates")
  .setGroupIds(List.of("b81eceff-d8db-4e10-a10d-01010f294fe9"))
  .setUserIds(List.of(
    "08bc7305-5d85-424b-b6ab-0007d35d501f",
    "1a66d10c-dcba-4d86-8bf0-000c5b7086c2"
  ))
  .build();

TemplateFolder folder = client.v1().templateFolders().create(params);`,
    'Go': `twc := techwriter.NewClient("tw_test_51H59...")

params := &techwriter.TemplateFolderParams{
  Name: "Onboarding Templates",
  GroupIds: []string{"b81eceff-d8db-4e10-a10d-01010f294fe9"},
  UserIds: []string{
    "08bc7305-5d85-424b-b6ab-0007d35d501f",
    "1a66d10c-dcba-4d86-8bf0-000c5b7086c2",
  },
}
folder, err := twc.TemplateFolders.Create(ctx, params)`,
    '.NET': `var client = new TechWriterClient("tw_test_51H59...");

var options = new TemplateFolderCreateOptions {
  Name = "Onboarding Templates",
  GroupIds = new List<string> { "b81eceff-d8db-4e10-a10d-01010f294fe9" },
  UserIds = new List<string> {
    "08bc7305-5d85-424b-b6ab-0007d35d501f",
    "1a66d10c-dcba-4d86-8bf0-000c5b7086c2"
  }
};
var folder = client.V1.TemplateFolders.Create(options);`
  };

  const responseExamples = {
    '201': {
      label: '201 Created',
      desc: 'Template folder was created successfully. The response includes the new folder ID and creation timestamp.',
      code: `{
  "data": {
    "folderId": "8a921f63-e90b-4296-83b5-2a793817e778",
    "name": "Onboarding Templates",
    "createdDate": "2026-01-30T08:53:05.1504161-08:00"
  },
  "success": true
}`
    },
    '400': {
      label: '400 Bad Request',
      desc: 'The payload is malformed: missing name, or non-string IDs in groupIds / userIds.',
      code: `{
  "error": {
    "code": "invalid_request",
    "message": "The 'name' field is required and must be a non-empty string.",
    "param": "name"
  },
  "success": false
}`
    },
    '401': {
      label: '401 Unauthorized',
      desc: 'Authentication failed: missing, invalid, or expired bearer token.',
      code: `{
  "error": {
    "code": "unauthorized",
    "message": "Authentication credentials are missing or invalid."
  },
  "success": false
}`
    },
    '403': {
      label: '403 Forbidden',
      desc: 'The token is valid but lacks the templates:write scope.',
      code: `{
  "error": {
    "code": "forbidden",
    "message": "Your API key does not have the 'templates:write' scope.",
    "requiredScope": "templates:write"
  },
  "success": false
}`
    },
    '409': {
      label: '409 Conflict',
      desc: 'A template folder with the same name already exists at the root level.',
      code: `{
  "error": {
    "code": "folder_name_conflict",
    "message": "A template folder named 'Onboarding Templates' already exists.",
    "param": "name"
  },
  "success": false
}`
    },
    '422': {
      label: '422 Unprocessable',
      desc: 'One or more group or user IDs could not be resolved in the workspace.',
      code: `{
  "error": {
    "code": "unprocessable_entity",
    "message": "One or more provided userIds were not found in the workspace.",
    "param": "userIds",
    "missingIds": ["1a66d10c-dcba-4d86-8bf0-000c5b7086c2"]
  },
  "success": false
}`
    },
    '429': {
      label: '429 Too Many',
      desc: 'You exceeded the rate limit. Retry after the specified delay.',
      code: `{
  "error": {
    "code": "rate_limited",
    "message": "Too many requests. Please retry after the cool-down period.",
    "retryAfterSeconds": 30
  },
  "success": false
}`
    },
    '500': {
      label: '500 Server Error',
      desc: 'An unexpected error occurred while creating the folder.',
      code: `{
  "error": {
    "code": "internal_error",
    "message": "An unexpected error occurred. Our team has been notified.",
    "requestId": "req_3c1a...e87"
  },
  "success": false
}`
    },
    '503': {
      label: '503 Unavailable',
      desc: 'The service is temporarily down for maintenance or overloaded.',
      code: `{
  "error": {
    "code": "service_unavailable",
    "message": "The service is temporarily unavailable. Try again in a few moments.",
    "retryAfterSeconds": 60
  },
  "success": false
}`
    }
  };

  const statusOrder = ['201', '400', '401', '403', '409', '422', '429', '500', '503'];

  const statusAccent = (s) => {
    if (s.startsWith('2')) return '#3fb950';
    if (s.startsWith('4')) return '#F68B1F';
    if (s.startsWith('5')) return '#f85149';
    return '#88888f';
  };

  const cardTableStyle = { marginBottom: '3rem', border: '1px solid #1e1e20', borderRadius: '0.5rem', overflow: 'hidden' };
  const headerCellsStyle = (cols) => ({
    display: 'grid',
    gridTemplateColumns: cols,
    gap: '1rem',
    padding: '0.75rem 1rem',
    backgroundColor: '#1a1a1c',
    borderBottom: '1px solid #303033',
    fontSize: '11px',
    fontWeight: '700',
    textTransform: 'uppercase',
    letterSpacing: '0.08em',
    color: '#88888f'
  });
  const bodyRowStyle = (cols, isLast) => ({
    display: 'grid',
    gridTemplateColumns: cols,
    gap: '1rem',
    padding: '0.875rem 1rem',
    borderBottom: isLast ? 'none' : '1px solid #1e1e20',
    alignItems: 'baseline',
    fontSize: '13px'
  });

  const headerRows = [
    { name: 'Authorization', value: 'Bearer {YOUR_BEARER_TOKEN}', required: true, desc: 'Access token used to authenticate the API request.' },
    { name: 'Accept', value: '*/*', required: false, desc: 'Indicates the response formats the client can accept.' },
    { name: 'Content-Type', value: 'application/json', required: true, desc: 'Indicates that the request body is JSON.' }
  ];

  const bodyFieldRows = [
    { name: 'name', type: 'string', required: true, desc: 'Display name of the new template folder. Must be unique among root-level folders.' },
    { name: 'groupIds', type: 'string[]', required: false, desc: 'Optional list of user-group IDs that should receive access to the folder.' },
    { name: 'userIds', type: 'string[]', required: false, desc: 'Optional list of user IDs that should receive direct access to the folder.' }
  ];

  const responseFieldRows = [
    { name: 'data.folderId', type: 'string', desc: 'Unique identifier of the new template folder.' },
    { name: 'data.name', type: 'string', desc: 'Display name of the created folder, echoed back from the request.' },
    { name: 'data.createdDate', type: 'string', desc: 'Folder creation timestamp in ISO 8601 format.' },
    { name: 'success', type: 'boolean', desc: 'Indicates whether the request completed successfully.' }
  ];

  const copyToClipboard = async (text, id) => {
    try {
      await navigator.clipboard.writeText(text);
      setCopiedSnippets({ ...copiedSnippets, [id]: true });
      setTimeout(() => setCopiedSnippets((prev) => ({ ...prev, [id]: false })), 2000);
    } catch (err) {
      console.error('Failed to copy!', err);
    }
  };

  const renderCode = (snippet, id) => (
    <div style={{ marginBottom: '0', position: 'relative' }}>
      <div style={{ position: 'absolute', right: '16px', top: '16px', zIndex: 10 }}>
        <button
          onClick={() => copyToClipboard(snippet, id)}
          style={{
            background: '#1a1a1c',
            color: '#555',
            padding: '8px',
            borderRadius: '4px',
            border: '1px solid #303033',
            cursor: 'pointer',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center'
          }}
          title="Copy code"
        >
          {copiedSnippets[id] ? <CheckIcon style={{ color: '#F68B1F' }} /> : <CopyIcon />}
        </button>
      </div>
      <div style={{
        padding: '32px',
        fontFamily: 'monospace',
        fontSize: '13px',
        backgroundColor: '#0d0d0f',
        color: '#e3e3e8',
        lineHeight: '1.6',
        overflowX: 'auto',
        border: '1px solid #303033',
        borderRadius: '12px',
        boxShadow: 'inset 0 2px 4px rgba(0,0,0,0.3)'
      }}>
        <HighlightedCode code={snippet} />
      </div>
    </div>
  );

  const [isMobileView, setIsMobileView] = React.useState(
    typeof window !== 'undefined' ? window.innerWidth < 768 : false
  );
  React.useEffect(() => {
    const handler = () => setIsMobileView(window.innerWidth < 768);
    window.addEventListener('resize', handler);
    return () => window.removeEventListener('resize', handler);
  }, []);

  return (
    <div style={{
      display: 'flex',
      flexDirection: isMobileView ? 'column' : 'row',
      minHeight: '100vh',
      backgroundColor: 'var(--ifm-background-color)',
      margin: isMobileView ? '0' : '-2rem',
      borderTop: '1px solid #1e1e20'
    }}>

      {/* Left Column: Primary Content */}
      <div style={{
        flex: '1 1 0%',
        padding: isMobileView ? '1.5rem 1rem' : '4rem',
        color: 'var(--ifm-font-color-base)',
        borderRight: isMobileView ? 'none' : '1px solid #1e1e20'
      }}>
        <div style={{ maxWidth: '760px' }}>

          {/* Header */}
          <div style={{ display: 'flex', alignItems: 'center', gap: '0.75rem', marginBottom: '1rem' }}>
            <span style={{ backgroundColor: 'rgba(246, 139, 31, 0.12)', color: '#F68B1F', padding: '0.25rem 0.625rem', borderRadius: '0.375rem', fontFamily: 'monospace', fontSize: '12px', fontWeight: '700', letterSpacing: '0.05em', border: '1px solid rgba(246, 139, 31, 0.35)' }}>POST</span>
            <code style={{ fontSize: '13px', color: '#adadb8', fontFamily: 'monospace' }}>/api/public/template-folders</code>
          </div>
          <h1 style={{ fontSize: '2rem', fontWeight: 'bold', color: 'var(--ifm-font-color-base)', marginBottom: '1.5rem', letterSpacing: '-0.025em' }}>Create a template folder</h1>

          <div style={{ display: 'flex', flexDirection: 'column', gap: '1.5rem', fontSize: '15px', lineHeight: '1.75', marginBottom: '3rem' }}>
            <p>
              Creates a new folder inside the <span style={{ color: '#F68B1F', fontWeight: '500' }}>TechWriter</span> Template Library. Provide a folder <code style={{ backgroundColor: '#1e1e20', padding: '0.125rem 0.375rem', borderRadius: '0.25rem', fontFamily: 'monospace', color: '#e3e3e8', fontSize: '13px' }}>name</code> and, optionally, the user groups and individual users who should have access. On success the API returns the new folder ID along with the creation timestamp.
            </p>

            <p style={{ borderLeft: '2px solid #F68B1F', paddingLeft: '1.5rem', fontStyle: 'italic', color: '#adadb8' }}>
              Folder names must be unique among root-level folders in the workspace. Use <code style={{ backgroundColor: '#1e1e20', padding: '0.125rem 0.375rem', borderRadius: '0.25rem', fontFamily: 'monospace', color: '#e3e3e8', fontSize: '13px' }}>groupIds</code> for team-wide access and <code style={{ backgroundColor: '#1e1e20', padding: '0.125rem 0.375rem', borderRadius: '0.25rem', fontFamily: 'monospace', color: '#e3e3e8', fontSize: '13px' }}>userIds</code> for individual exceptions — both arrays may be empty if the folder is private to the creator.
            </p>
          </div>

          {/* Query parameters */}
          <h2 style={{ fontSize: '20px', fontWeight: 'bold', color: 'var(--ifm-font-color-base)', marginBottom: '0.75rem', letterSpacing: '-0.02em' }}>Query parameters</h2>
          <p style={{ fontSize: '14px', color: '#adadb8', marginBottom: '3rem' }}>None — this endpoint does not accept any query string parameters.</p>

          {/* Request Headers */}
          <h2 style={{ fontSize: '20px', fontWeight: 'bold', color: 'var(--ifm-font-color-base)', marginBottom: '0.75rem', letterSpacing: '-0.02em' }}>Request headers</h2>
          <div style={cardTableStyle}>
            <div style={headerCellsStyle('170px 1fr 90px 1.4fr')}>
              <span>Header</span>
              <span>Value</span>
              <span>Required</span>
              <span>Description</span>
            </div>
            {headerRows.map((h, idx) => (
              <div key={h.name} style={bodyRowStyle('170px 1fr 90px 1.4fr', idx === headerRows.length - 1)}>
                <code style={{ fontFamily: 'monospace', color: '#F68B1F', fontWeight: '600', fontSize: '13px' }}>{h.name}</code>
                <code style={{ fontFamily: 'monospace', color: '#e3e3e8', fontSize: '12px' }}>{h.value}</code>
                <span style={{ fontSize: '11px', fontWeight: '700', textTransform: 'uppercase', letterSpacing: '0.05em', color: h.required ? '#f85149' : '#6b6b7b' }}>{h.required ? 'required' : 'optional'}</span>
                <span style={{ color: '#adadb8', lineHeight: '1.5' }}>{h.desc}</span>
              </div>
            ))}
          </div>

          {/* Request Body */}
          <h2 style={{ fontSize: '20px', fontWeight: 'bold', color: 'var(--ifm-font-color-base)', marginBottom: '0.75rem', letterSpacing: '-0.02em' }}>Request body</h2>
          <p style={{ fontSize: '14px', color: '#adadb8', marginBottom: '1rem' }}>Send a JSON object with the folder name and (optionally) the groups or users that should receive access.</p>
          <div style={cardTableStyle}>
            <div style={headerCellsStyle('200px 130px 90px 1fr')}>
              <span>Field</span>
              <span>Type</span>
              <span>Required</span>
              <span>Description</span>
            </div>
            {bodyFieldRows.map((f, idx) => (
              <div key={f.name} style={bodyRowStyle('200px 130px 90px 1fr', idx === bodyFieldRows.length - 1)}>
                <code style={{ fontFamily: 'monospace', color: '#e3e3e8', fontSize: '13px' }}>{f.name}</code>
                <span style={{ fontFamily: 'monospace', color: '#F68B1F', fontSize: '12px', fontWeight: '600' }}>{f.type}</span>
                <span style={{ fontSize: '11px', fontWeight: '700', textTransform: 'uppercase', letterSpacing: '0.05em', color: f.required ? '#f85149' : '#6b6b7b' }}>{f.required ? 'required' : 'optional'}</span>
                <span style={{ color: '#adadb8', lineHeight: '1.5' }}>{f.desc}</span>
              </div>
            ))}
          </div>

          {/* Response Fields */}
          <h2 style={{ fontSize: '20px', fontWeight: 'bold', color: 'var(--ifm-font-color-base)', marginBottom: '0.75rem', letterSpacing: '-0.02em' }}>Response fields</h2>
          <p style={{ fontSize: '14px', color: '#adadb8', marginBottom: '1rem' }}>Returned on a successful <code style={{ backgroundColor: '#1e1e20', padding: '0.125rem 0.375rem', borderRadius: '0.25rem', fontFamily: 'monospace', color: '#e3e3e8', fontSize: '13px' }}>201 Created</code> response.</p>
          <div style={cardTableStyle}>
            <div style={headerCellsStyle('230px 110px 1fr')}>
              <span>Field</span>
              <span>Type</span>
              <span>Description</span>
            </div>
            {responseFieldRows.map((f, idx) => (
              <div key={f.name} style={bodyRowStyle('230px 110px 1fr', idx === responseFieldRows.length - 1)}>
                <code style={{ fontFamily: 'monospace', color: '#e3e3e8', fontSize: '13px' }}>{f.name}</code>
                <span style={{ fontFamily: 'monospace', color: '#F68B1F', fontSize: '12px', fontWeight: '600' }}>{f.type}</span>
                <span style={{ color: '#adadb8', lineHeight: '1.5' }}>{f.desc}</span>
              </div>
            ))}
          </div>

          {/* Response Codes */}
          <h2 style={{ fontSize: '20px', fontWeight: 'bold', color: 'var(--ifm-font-color-base)', marginBottom: '0.75rem', letterSpacing: '-0.02em' }}>Response codes</h2>
          <p style={{ fontSize: '14px', color: '#adadb8', marginBottom: '1rem' }}>The API uses conventional HTTP status codes. <code style={{ backgroundColor: '#1e1e20', padding: '0.125rem 0.375rem', borderRadius: '0.25rem', fontFamily: 'monospace', color: '#e3e3e8', fontSize: '13px' }}>2xx</code> indicate success, <code style={{ backgroundColor: '#1e1e20', padding: '0.125rem 0.375rem', borderRadius: '0.25rem', fontFamily: 'monospace', color: '#e3e3e8', fontSize: '13px' }}>4xx</code> indicate client errors, and <code style={{ backgroundColor: '#1e1e20', padding: '0.125rem 0.375rem', borderRadius: '0.25rem', fontFamily: 'monospace', color: '#e3e3e8', fontSize: '13px' }}>5xx</code> indicate server-side issues.</p>
          <div style={cardTableStyle}>
            <div style={headerCellsStyle('170px 1fr')}>
              <span>Status</span>
              <span>Description</span>
            </div>
            {statusOrder.map((s, idx) => (
              <div key={s} style={bodyRowStyle('170px 1fr', idx === statusOrder.length - 1)}>
                <span style={{ fontFamily: 'monospace', color: statusAccent(s), fontSize: '13px', fontWeight: '700' }}>{responseExamples[s].label}</span>
                <span style={{ color: '#adadb8', lineHeight: '1.5' }}>{responseExamples[s].desc}</span>
              </div>
            ))}
          </div>

          {/* Feedback Section */}
          <div style={{ marginTop: '4rem', paddingTop: '3rem', borderTop: '1px solid #1e1e20' }}>
            {!feedback ? (
              <div style={{ display: 'flex', alignItems: 'center', gap: '2rem' }}>
                <span style={{ fontSize: '14px', fontWeight: '500', color: '#88888f' }}>Was this section helpful?</span>
                <button onClick={() => setFeedback('yes')} style={{ color: '#F68B1F', fontSize: '14px', fontWeight: '600', background: 'none', border: 'none', cursor: 'pointer' }}>Yes</button>
                <button onClick={() => setFeedback('no')} style={{ color: '#F68B1F', fontSize: '14px', fontWeight: '600', background: 'none', border: 'none', cursor: 'pointer' }}>No</button>
              </div>
            ) : (
              <div style={{ animation: 'fadeIn 0.5s' }}>
                {feedback === 'yes' ? (
                  <p style={{ fontSize: '14px', color: '#e3e3e8', backgroundColor: '#1a1a1c', padding: '0.75rem 1.5rem', borderRadius: '0.375rem', display: 'inline-block', border: '1px solid #303033' }}>
                    ✨ Thanks for your feedback! We're glad this helped.
                  </p>
                ) : (
                  <div style={{ backgroundColor: '#1a1a1c', padding: '1.5rem', borderRadius: '0.375rem', border: '1px solid #303033', maxWidth: '448px' }}>
                    <p style={{ fontSize: '14px', color: '#e3e3e8', marginBottom: '1rem' }}>We're sorry! How can we improve this page?</p>
                    <textarea
                      style={{ width: '100%', backgroundColor: '#0a0a0a', border: '1px solid #303033', borderRadius: '0.5rem', padding: '0.75rem', fontSize: '0.875rem', color: '#adadb8', outline: 'none' }}
                      placeholder="Your feedback..."
                      rows="4"
                    ></textarea>
                    <button style={{ marginTop: '1rem', backgroundColor: '#F68B1F', color: 'white', fontSize: '0.75rem', fontWeight: 'bold', padding: '0.5rem 1rem', borderRadius: '0.5rem', border: 'none', cursor: 'pointer' }}>Submit Feedback</button>
                  </div>
                )}
              </div>
            )}
            <div style={{ paddingBottom: '8rem' }}></div>
          </div>
        </div>
      </div>

      {/* Right Column: Code and Responses */}
      <div style={{ width: isMobileView ? '100%' : '45%', backgroundColor: 'rgba(0,0,0,0.02)', padding: isMobileView ? '1.5rem 1rem' : '3rem', borderLeft: isMobileView ? 'none' : '1px solid #1e1e20', borderTop: isMobileView ? '1px solid #1e1e20' : 'none' }}>
        <div style={{ position: isMobileView ? 'static' : 'sticky', top: isMobileView ? 'auto' : '3rem', display: 'flex', flexDirection: 'column', gap: '2rem' }}>

          {/* Language Picker */}
          <div style={{ position: 'relative' }}>
            <button
              onClick={() => setIsOpen(!isOpen)}
              style={{
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'space-between',
                width: '100%',
                backgroundColor: '#1a1a1c',
                padding: '0.75rem 1.25rem',
                borderRadius: '0.5rem',
                fontSize: '14px',
                color: 'white',
                border: '1px solid #303033',
                cursor: 'pointer'
              }}
            >
              <span style={{ fontWeight: '600' }}>{language}</span>
              <ChevronDown size={16} style={{ color: '#88888f', transform: isOpen ? 'rotate(180deg)' : 'rotate(0)' }} />
            </button>

            {isOpen && (
              <div style={{ position: 'absolute', width: '100%', marginTop: '0.5rem', backgroundColor: '#1a1a1c', border: '1px solid #303033', borderRadius: '0.75rem', boxShadow: '0 25px 50px -12px rgba(0, 0, 0, 0.5)', zIndex: 50, padding: '0.5rem 0', overflow: 'hidden' }}>
                {languages.map((lang) => (
                  <button
                    key={lang.id}
                    style={{
                      width: '100%',
                      textAlign: 'left',
                      padding: '0.625rem 1.25rem',
                      fontSize: '13px',
                      backgroundColor: language === lang.name ? '#F68B1F' : 'transparent',
                      color: language === lang.name ? 'white' : '#adadb8',
                      border: 'none',
                      cursor: 'pointer'
                    }}
                    onClick={() => {
                      setLanguage(lang.name);
                      setIsOpen(false);
                    }}
                  >
                    {lang.name}
                  </button>
                ))}
              </div>
            )}
          </div>

          {/* Request snippet */}
          <div>
            <h3 style={{ color: 'var(--ifm-font-color-base)', fontSize: '13px', fontWeight: 'bold', textTransform: 'uppercase', letterSpacing: '0.08em', marginBottom: '0.875rem' }}>Example request</h3>
            {renderCode(requestSnippets[language], 'request')}
          </div>

          {/* Response status switcher */}
          <div>
            <h3 style={{ color: 'var(--ifm-font-color-base)', fontSize: '13px', fontWeight: 'bold', textTransform: 'uppercase', letterSpacing: '0.08em', marginBottom: '0.875rem' }}>Example response</h3>
            <div style={{ display: 'flex', flexWrap: 'wrap', gap: '0.5rem', marginBottom: '1rem' }}>
              {statusOrder.map((s) => {
                const active = activeStatus === s;
                return (
                  <button
                    key={s}
                    onClick={() => setActiveStatus(s)}
                    style={{
                      backgroundColor: active ? statusAccent(s) : '#1a1a1c',
                      color: active ? 'white' : '#adadb8',
                      border: `1px solid ${active ? statusAccent(s) : '#303033'}`,
                      padding: '0.4rem 0.75rem',
                      borderRadius: '0.375rem',
                      fontSize: '12px',
                      fontFamily: 'monospace',
                      fontWeight: '600',
                      cursor: 'pointer',
                      transition: 'all 0.15s'
                    }}
                  >
                    {s}
                  </button>
                );
              })}
            </div>
            <div style={{ marginBottom: '0.75rem', fontSize: '13px', color: '#adadb8', lineHeight: '1.5' }}>
              <span style={{ color: statusAccent(activeStatus), fontFamily: 'monospace', fontWeight: '700', marginRight: '0.5rem' }}>{responseExamples[activeStatus].label}</span>
              {responseExamples[activeStatus].desc}
            </div>
            {renderCode(responseExamples[activeStatus].code, `resp-${activeStatus}`)}
          </div>

          {/* Tip Card */}
          <div style={{ backgroundColor: '#1a1a1c', borderRadius: '0.75rem', border: '1px solid #303033', padding: '1.5rem', display: 'flex', flexDirection: 'column', gap: '1rem', borderLeft: '4px solid #F68B1F', boxShadow: '0 20px 25px -5px rgba(0, 0, 0, 0.1)' }}>
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
              <h3 style={{ fontSize: '11px', fontWeight: 'bold', textTransform: 'uppercase', letterSpacing: '0.1em', color: '#88888f', margin: 0 }}>Granting access later</h3>
              <Sparkles size={18} style={{ color: '#F68B1F' }} />
            </div>
            <p style={{ fontSize: '12px', lineHeight: '1.6', color: '#88888f', margin: 0 }}>
              You don't have to assign access at creation time. Send an empty <code style={{ color: '#e3e3e8', fontFamily: 'monospace' }}>groupIds</code> and <code style={{ color: '#e3e3e8', fontFamily: 'monospace' }}>userIds</code> to keep the folder private, then share it later through the share endpoint or the Template Library UI.
            </p>
          </div>

        </div>
      </div>
    </div>
  );
};

<PostCreateTemplateFolderPage />
