---
sidebar_position: 1
title: Industrial Automation
description: ISA-95 automation hierarchy, types of automation, key technologies, industrial communication protocols, safety standards, and Industry 4.0.
hide_table_of_contents: true
---

import React, { useState, useEffect } from 'react';

export const FactoryIcon = ({ size = 16 }) => (
  <svg xmlns="http://www.w3.org/2000/svg" width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
    <path d="M2 20a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V8l-7 5V8l-7 5V4a2 2 0 0 0-2-2H4a2 2 0 0 0-2 2Z"/>
    <path d="M17 18h1"/><path d="M12 18h1"/><path d="M7 18h1"/>
  </svg>
);

export const NetworkIcon = ({ size = 16 }) => (
  <svg xmlns="http://www.w3.org/2000/svg" width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
    <rect x="16" y="16" width="6" height="6" rx="1"/><rect x="2" y="16" width="6" height="6" rx="1"/>
    <rect x="9" y="2" width="6" height="6" rx="1"/>
    <path d="M5 16v-3a1 1 0 0 1 1-1h12a1 1 0 0 1 1 1v3"/><path d="M12 12V8"/>
  </svg>
);

export const ShieldIcon = ({ size = 16 }) => (
  <svg xmlns="http://www.w3.org/2000/svg" width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
  </svg>
);

export const ZapIcon = ({ size = 16 }) => (
  <svg xmlns="http://www.w3.org/2000/svg" width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
    <path d="M4 14a1 1 0 0 1-.78-1.63l9.9-10.2a.5.5 0 0 1 .86.46l-1.92 6.02A1 1 0 0 0 13 10h7a1 1 0 0 1 .78 1.63l-9.9 10.2a.5.5 0 0 1-.86-.46l1.92-6.02A1 1 0 0 0 11 14z"/>
  </svg>
);

export const IndAutPage = () => {
  const [isMobileView, setIsMobileView] = useState(false);
  const [activeTab, setActiveTab] = useState('pyramid');
  const [feedback, setFeedback] = useState(null);

  useEffect(() => {
    const check = () => setIsMobileView(window.innerWidth < 900);
    check();
    window.addEventListener('resize', check);
    return () => window.removeEventListener('resize', check);
  }, []);

  const sectionTitle = (text) => (
    <h2 style={{ color: 'var(--ifm-font-color-base)', fontSize: '15px', fontWeight: '700', marginBottom: '1rem', marginTop: '2.5rem', paddingBottom: '0.5rem', borderBottom: '1px solid #1e1e20', letterSpacing: '-0.01em' }}>
      {text}
    </h2>
  );

  const Chip = ({ label, icon, accent = '#F68B1F' }) => (
    <div style={{ display: 'flex', alignItems: 'center', gap: '6px', backgroundColor: '#1a1a1c', border: '1px solid #303033', borderRadius: '8px', padding: '7px 14px' }}>
      <span style={{ color: accent }}>{icon}</span>
      <span style={{ fontSize: '12px', fontWeight: '600', color: '#e3e3e8' }}>{label}</span>
    </div>
  );

  const tabs = [
    { id: 'pyramid', label: 'Pyramid' },
    { id: 'architecture', label: 'Architecture' },
    { id: 'protocols', label: 'Protocols' },
  ];

  // ── Automation Pyramid SVG ──
  const AutomationPyramid = () => (
    <div>
      <svg viewBox="0 0 420 335" xmlns="http://www.w3.org/2000/svg" style={{ width: '100%', display: 'block' }}>
        {/* Level 0 — Field Level */}
        <polygon points="0,325 420,325 392,275 28,275" fill="#0b2310" stroke="#1b5c22" strokeWidth="1.5"/>
        <text x="210" y="300" textAnchor="middle" fill="#4ade80" fontSize="10.5" fontWeight="700">LEVEL 0 — FIELD LEVEL</text>
        <text x="210" y="315" textAnchor="middle" fill="#2d8a3d" fontSize="9">Sensors · Actuators · Drives · Valves · Motors · Instruments</text>

        {/* Level 1 — Control Level */}
        <polygon points="28,275 392,275 358,225 62,225" fill="#0c1c2e" stroke="#1b4b72" strokeWidth="1.5"/>
        <text x="210" y="251" textAnchor="middle" fill="#60a5fa" fontSize="10.5" fontWeight="700">LEVEL 1 — CONTROL LEVEL</text>
        <text x="210" y="266" textAnchor="middle" fill="#3676b8" fontSize="9">PLC · RTU · DCS · Safety Controllers · PAC</text>

        {/* Level 2 — Supervisory Level */}
        <polygon points="62,225 358,225 324,175 96,175" fill="#180f2e" stroke="#4a2472" strokeWidth="1.5"/>
        <text x="210" y="201" textAnchor="middle" fill="#c084fc" fontSize="10.5" fontWeight="700">LEVEL 2 — SUPERVISORY</text>
        <text x="210" y="216" textAnchor="middle" fill="#7a3ec8" fontSize="9">SCADA · HMI · Data Historian · Alarm Management</text>

        {/* Level 3 — MES Level */}
        <polygon points="96,175 324,175 290,125 130,125" fill="#2a1b0a" stroke="#6e4814" strokeWidth="1.5"/>
        <text x="210" y="152" textAnchor="middle" fill="#fb923c" fontSize="10.5" fontWeight="700">LEVEL 3 — MES / MOM</text>
        <text x="210" y="167" textAnchor="middle" fill="#a85e20" fontSize="9">Manufacturing Execution · OEE · Quality · Scheduling</text>

        {/* Level 4 — Enterprise Level */}
        <polygon points="130,125 290,125 210,55" fill="#2a100a" stroke="#6e2418" strokeWidth="1.5"/>
        <text x="210" y="96" textAnchor="middle" fill="#F68B1F" fontSize="10.5" fontWeight="700">LEVEL 4 — ERP</text>
        <text x="210" y="113" textAnchor="middle" fill="#a85010" fontSize="8.5">Enterprise · Cloud · BI</text>

        {/* Side level badges */}
        {[['L0',308,'#4ade80'],['L1',253,'#60a5fa'],['L2',201,'#c084fc'],['L3',151,'#fb923c'],['L4',97,'#F68B1F']].map(([lbl,y,col]) => (
          <text key={lbl} x="6" y={y} fill={col} fontSize="9" fontStyle="italic" fontWeight="700">{lbl}</text>
        ))}
      </svg>
      <div style={{ display: 'flex', justifyContent: 'space-between', marginTop: '0.5rem', padding: '0 0.25rem' }}>
        <span style={{ fontSize: '10px', color: '#88888f' }}>ISA-95 / IEC 62264 — Five-Level Model</span>
        <span style={{ fontSize: '10px', color: '#4ade80' }}>▲ Field → Enterprise</span>
      </div>
    </div>
  );

  // ── System Architecture Block Diagram ──
  const SystemArchitecture = () => (
    <div>
      <svg viewBox="0 0 480 210" xmlns="http://www.w3.org/2000/svg" style={{ width: '100%', display: 'block' }}>
        <defs>
          <marker id="iarrow" markerWidth="7" markerHeight="5" refX="7" refY="2.5" orient="auto">
            <polygon points="0 0, 7 2.5, 0 5" fill="#F68B1F"/>
          </marker>
        </defs>

        {/* FIELD */}
        <rect x="4" y="70" width="80" height="65" rx="6" fill="#0b2310" stroke="#1b5c22" strokeWidth="1.5"/>
        <text x="44" y="94" textAnchor="middle" fill="#4ade80" fontSize="9" fontWeight="700">FIELD</text>
        <text x="44" y="107" textAnchor="middle" fill="#2d8a3d" fontSize="8">Sensors</text>
        <text x="44" y="119" textAnchor="middle" fill="#2d8a3d" fontSize="8">Actuators</text>
        <text x="44" y="127" textAnchor="middle" fill="#1d5a27" fontSize="7.5">L0</text>

        <line x1="84" y1="102" x2="100" y2="102" stroke="#F68B1F" strokeWidth="1.5" markerEnd="url(#iarrow)"/>

        {/* PLC / DCS */}
        <rect x="101" y="70" width="80" height="65" rx="6" fill="#0c1c2e" stroke="#1b4b72" strokeWidth="1.5"/>
        <text x="141" y="94" textAnchor="middle" fill="#60a5fa" fontSize="9" fontWeight="700">PLC / DCS</text>
        <text x="141" y="107" textAnchor="middle" fill="#3676b8" fontSize="8">Control</text>
        <text x="141" y="119" textAnchor="middle" fill="#3676b8" fontSize="8">Logic</text>
        <text x="141" y="127" textAnchor="middle" fill="#1d4a6a" fontSize="7.5">L1</text>

        <line x1="181" y1="102" x2="197" y2="102" stroke="#F68B1F" strokeWidth="1.5" markerEnd="url(#iarrow)"/>

        {/* SCADA / HMI */}
        <rect x="198" y="70" width="80" height="65" rx="6" fill="#180f2e" stroke="#4a2472" strokeWidth="1.5"/>
        <text x="238" y="94" textAnchor="middle" fill="#c084fc" fontSize="9" fontWeight="700">SCADA</text>
        <text x="238" y="107" textAnchor="middle" fill="#7a3ec8" fontSize="8">HMI</text>
        <text x="238" y="119" textAnchor="middle" fill="#7a3ec8" fontSize="8">Historian</text>
        <text x="238" y="127" textAnchor="middle" fill="#4a1a88" fontSize="7.5">L2</text>

        <line x1="278" y1="102" x2="294" y2="102" stroke="#F68B1F" strokeWidth="1.5" markerEnd="url(#iarrow)"/>

        {/* MES */}
        <rect x="295" y="70" width="80" height="65" rx="6" fill="#2a1b0a" stroke="#6e4814" strokeWidth="1.5"/>
        <text x="335" y="94" textAnchor="middle" fill="#fb923c" fontSize="9" fontWeight="700">MES</text>
        <text x="335" y="107" textAnchor="middle" fill="#a85e20" fontSize="8">Production</text>
        <text x="335" y="119" textAnchor="middle" fill="#a85e20" fontSize="8">Scheduling</text>
        <text x="335" y="127" textAnchor="middle" fill="#6a3a10" fontSize="7.5">L3</text>

        <line x1="375" y1="102" x2="391" y2="102" stroke="#F68B1F" strokeWidth="1.5" markerEnd="url(#iarrow)"/>

        {/* ERP / Cloud */}
        <rect x="392" y="70" width="80" height="65" rx="6" fill="#2a100a" stroke="#6e2418" strokeWidth="1.5"/>
        <text x="432" y="94" textAnchor="middle" fill="#F68B1F" fontSize="9" fontWeight="700">ERP</text>
        <text x="432" y="107" textAnchor="middle" fill="#a85010" fontSize="8">Enterprise</text>
        <text x="432" y="119" textAnchor="middle" fill="#a85010" fontSize="8">Cloud / BI</text>
        <text x="432" y="127" textAnchor="middle" fill="#6a3010" fontSize="7.5">L4</text>

        {/* OT Network bar */}
        <line x1="4" y1="160" x2="181" y2="160" stroke="#1b5c22" strokeWidth="1" strokeDasharray="4,3"/>
        <text x="92" y="172" textAnchor="middle" fill="#2d8a3d" fontSize="8">Fieldbus / Industrial Ethernet (OT)</text>

        {/* IT Network bar */}
        <line x1="198" y1="160" x2="472" y2="160" stroke="#4a2472" strokeWidth="1" strokeDasharray="4,3"/>
        <text x="335" y="172" textAnchor="middle" fill="#7a3ec8" fontSize="8">IT Network / Cloud Infrastructure</text>

        {/* OT/IT label */}
        <text x="186" y="157" textAnchor="middle" fill="#F68B1F" fontSize="9" fontWeight="700">OT│IT</text>
      </svg>
      <div style={{ fontSize: '10px', color: '#88888f', marginTop: '0.25rem', textAlign: 'center' }}>
        Horizontal data flow — field device to enterprise cloud
      </div>
    </div>
  );

  // ── Protocol Speed Bar Chart ──
  const ProtocolChart = () => {
    const data = [
      { name: 'PROFINET IRT', speed: '1 Gbps',   pct: 100, color: '#1b4b72' },
      { name: 'EtherNet/IP', speed: '1 Gbps',    pct: 100, color: '#1b4b72' },
      { name: 'CC-Link IE',  speed: '1 Gbps',    pct: 100, color: '#1b5c22' },
      { name: 'PROFIBUS DP', speed: '12 Mbps',   pct: 62,  color: '#6e4814' },
      { name: 'CANopen',     speed: '1 Mbps',    pct: 40,  color: '#4a2472' },
      { name: 'DeviceNet',   speed: '500 kbps',  pct: 34,  color: '#4a2472' },
      { name: 'Modbus RTU',  speed: '115 kbps',  pct: 19,  color: '#2a1b0a' },
    ];
    return (
      <div style={{ display: 'flex', flexDirection: 'column', gap: '9px' }}>
        {data.map(p => (
          <div key={p.name} style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
            <span style={{ fontSize: '10px', color: '#adadb8', width: '88px', flexShrink: 0, textAlign: 'right', lineHeight: 1.2 }}>{p.name}</span>
            <div style={{ height: '18px', width: `${Math.round(p.pct * 2.2)}px`, backgroundColor: p.color, borderRadius: '3px', border: '1px solid rgba(255,255,255,0.06)', minWidth: '6px', transition: 'width 0.3s' }}/>
            <span style={{ fontSize: '9px', color: '#88888f', flexShrink: 0 }}>{p.speed}</span>
          </div>
        ))}
        <div style={{ marginTop: '4px', fontSize: '9px', color: '#88888f', paddingLeft: '96px' }}>
          ← Log-scale relative speed →
        </div>
      </div>
    );
  };

  return (
    <div style={{ display: 'flex', flexDirection: isMobileView ? 'column' : 'row', minHeight: '100vh' }}>

      {/* ═══════════ LEFT COLUMN ═══════════ */}
      <div style={{ width: isMobileView ? '100%' : '58%', padding: isMobileView ? '1.5rem 1rem' : '3rem', boxSizing: 'border-box' }}>

        {/* Page header */}
        <div style={{ marginBottom: '2rem' }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: '0.625rem', marginBottom: '1rem' }}>
            <span style={{ backgroundColor: '#F68B1F', padding: '3px 10px', borderRadius: '6px', fontSize: '11px', fontWeight: '700', color: '#000', letterSpacing: '0.04em' }}>OVERVIEW</span>
            <span style={{ fontSize: '11px', color: '#88888f' }}>ISA-95 · IEC 62264 · Industry 4.0</span>
          </div>
          <h1 style={{ fontSize: '26px', fontWeight: '800', color: 'var(--ifm-font-color-base)', marginBottom: '0.75rem', lineHeight: '1.2' }}>
            Industrial Automation
          </h1>
          <p style={{ fontSize: '14px', color: '#adadb8', lineHeight: '1.75', marginBottom: '1.5rem' }}>
            Industrial automation is the use of control systems, computers, robots, and information technologies to perform tasks in manufacturing, processing, and infrastructure with minimal or no human intervention. It spans from simple relay-based circuits to fully autonomous, AI-driven smart factories.
          </p>
          <div style={{ display: 'flex', flexWrap: 'wrap', gap: '0.625rem' }}>
            <Chip label="Up to 70% Cost Reduction" icon={<ZapIcon size={12}/>} accent="#F68B1F"/>
            <Chip label="5 Hierarchy Levels (ISA-95)" icon={<NetworkIcon size={12}/>} accent="#79c0ff"/>
            <Chip label="Safety to SIL 4" icon={<ShieldIcon size={12}/>} accent="#4ade80"/>
          </div>
        </div>

        {/* Definition & Scope */}
        {sectionTitle('Definition & Scope')}
        <p style={{ fontSize: '14px', color: '#adadb8', lineHeight: '1.75', marginBottom: '1rem' }}>
          Industrial automation encompasses every technology that monitors and controls physical processes without continuous human involvement. The primary goals are to increase <strong style={{ color: '#e3e3e8' }}>throughput</strong>, improve <strong style={{ color: '#e3e3e8' }}>quality consistency</strong>, enhance <strong style={{ color: '#e3e3e8' }}>worker safety</strong>, reduce <strong style={{ color: '#e3e3e8' }}>operational costs</strong>, and enable 24/7 operation.
        </p>
        <p style={{ fontSize: '14px', color: '#adadb8', lineHeight: '1.75', marginBottom: '1rem' }}>
          Modern industrial automation integrates <strong style={{ color: '#e3e3e8' }}>Operational Technology (OT)</strong> — hardware and software that monitors and controls physical processes — with <strong style={{ color: '#e3e3e8' }}>Information Technology (IT)</strong> systems for analytics, enterprise resource planning, and cloud connectivity. This convergence is often called the <em>Digital Thread</em>.
        </p>

        {/* Types of Automation */}
        {sectionTitle('Types of Industrial Automation')}
        <div style={{ overflowX: 'auto', marginBottom: '1.5rem' }}>
          <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '13px' }}>
            <thead>
              <tr style={{ borderBottom: '1px solid #303033' }}>
                {['Type', 'Flexibility', 'Volume', 'Key Trait', 'Example'].map(h => (
                  <th key={h} style={{ padding: '8px 12px', textAlign: 'left', color: '#88888f', fontSize: '10px', fontWeight: '700', textTransform: 'uppercase', letterSpacing: '0.06em', whiteSpace: 'nowrap' }}>{h}</th>
                ))}
              </tr>
            </thead>
            <tbody>
              {[
                ['Fixed (Hard)', 'Very Low', 'Very High', 'Dedicated single-product line', 'Automotive body stamping'],
                ['Programmable', 'Medium', 'Medium–High', 'Reprogrammable between batches', 'CNC machining, PLC lines'],
                ['Flexible (FMS)', 'High', 'Low–Medium', 'Multi-product, rapid changeover', 'Robotic assembly cells'],
                ['Integrated (CIM)', 'Very High', 'Variable', 'Full computer-integrated factory', 'Lights-out smart factory'],
              ].map(([type, flex, vol, trait, ex], i) => (
                <tr key={i} style={{ borderBottom: '1px solid #1e1e20', backgroundColor: i % 2 === 0 ? 'transparent' : 'rgba(255,255,255,0.012)' }}>
                  <td style={{ padding: '8px 12px', color: '#e3e3e8', fontWeight: '600' }}>{type}</td>
                  <td style={{ padding: '8px 12px', color: '#adadb8' }}>{flex}</td>
                  <td style={{ padding: '8px 12px', color: '#adadb8' }}>{vol}</td>
                  <td style={{ padding: '8px 12px', color: '#adadb8' }}>{trait}</td>
                  <td style={{ padding: '8px 12px', color: '#F68B1F', fontFamily: 'monospace', fontSize: '12px' }}>{ex}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>

        {/* ISA-95 Hierarchy */}
        {sectionTitle('The ISA-95 Automation Hierarchy')}
        <p style={{ fontSize: '14px', color: '#adadb8', lineHeight: '1.75', marginBottom: '1rem' }}>
          The <strong style={{ color: '#e3e3e8' }}>ISA-95</strong> (IEC 62264) standard defines five functional levels, commonly visualised as a pyramid. Each level operates at a different time constant — from sub-millisecond at the field level to weekly planning cycles at the enterprise level.
        </p>
        <div style={{ display: 'flex', flexDirection: 'column', gap: '0.5rem', marginBottom: '1.5rem' }}>
          {[
            { level: 'L4', name: 'Enterprise (ERP)', desc: 'Business planning, supply chain, finance. SAP, Oracle, Microsoft Dynamics. Weeks/months time horizon.', color: '#F68B1F', bg: '#2a100a', border: '#6e2418' },
            { level: 'L3', name: 'MES / MOM', desc: 'Production scheduling, quality, OEE tracking, genealogy. SAP ME, Siemens Opcenter. Hours/days time horizon.', color: '#fb923c', bg: '#2a1b0a', border: '#6e4814' },
            { level: 'L2', name: 'Supervisory (SCADA/HMI)', desc: 'Plant-wide monitoring, alarm management, data historian. Wonderware, Ignition, WinCC. Seconds/minutes time horizon.', color: '#c084fc', bg: '#180f2e', border: '#4a2472' },
            { level: 'L1', name: 'Control (PLC / DCS)', desc: 'Real-time control loops, sequence logic, interlocks. Siemens S7, Allen-Bradley. Milliseconds time horizon.', color: '#60a5fa', bg: '#0c1c2e', border: '#1b4b72' },
            { level: 'L0', name: 'Field Level', desc: 'Physical sensors, actuators, drives, valves — the real world. Proximity switches, thermocouples, servo drives.', color: '#4ade80', bg: '#0b2310', border: '#1b5c22' },
          ].map(({ level, name, desc, color, bg, border }) => (
            <div key={level} style={{ display: 'flex', gap: '0.75rem', alignItems: 'flex-start', backgroundColor: bg, border: `1px solid ${border}`, borderRadius: '8px', padding: '10px 14px' }}>
              <span style={{ color, fontSize: '11px', fontWeight: '800', fontFamily: 'monospace', flexShrink: 0, paddingTop: '2px', minWidth: '22px' }}>{level}</span>
              <div>
                <span style={{ color, fontSize: '13px', fontWeight: '700' }}>{name}</span>
                <p style={{ color: '#88888f', fontSize: '12px', margin: '3px 0 0', lineHeight: '1.55' }}>{desc}</p>
              </div>
            </div>
          ))}
        </div>

        {/* Key Technologies */}
        {sectionTitle('Key Automation Technologies')}
        <div style={{ overflowX: 'auto', marginBottom: '1.5rem' }}>
          <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '12px' }}>
            <thead>
              <tr style={{ borderBottom: '1px solid #303033' }}>
                {['Technology', 'Abbrev.', 'Primary Function', 'Level'].map(h => (
                  <th key={h} style={{ padding: '8px 10px', textAlign: 'left', color: '#88888f', fontSize: '10px', fontWeight: '700', textTransform: 'uppercase', letterSpacing: '0.05em', whiteSpace: 'nowrap' }}>{h}</th>
                ))}
              </tr>
            </thead>
            <tbody>
              {[
                ['Programmable Logic Controller', 'PLC', 'Discrete & sequential control', 'L1'],
                ['Distributed Control System', 'DCS', 'Continuous process control (oil, chemical)', 'L1–L2'],
                ['Remote Terminal Unit', 'RTU', 'Field data acquisition over long distances', 'L0–L1'],
                ['Supervisory Control & Data Acq.', 'SCADA', 'Plant-wide monitoring and supervisory control', 'L2'],
                ['Human-Machine Interface', 'HMI', 'Operator display, control, and alarm panel', 'L1–L2'],
                ['Manufacturing Exec. System', 'MES', 'Production management and traceability', 'L3'],
                ['Industrial IoT', 'IIoT', 'Connected sensors, edge computing, cloud analytics', 'L0–L4'],
                ['Industrial Robotics', 'IR', 'Automated physical manipulation and assembly', 'L0–L1'],
                ['Safety Instrumented System', 'SIS', 'Emergency shutdown, fire & gas detection', 'L0–L1'],
                ['Condition Monitoring', 'CM', 'Predictive maintenance via vibration/thermal data', 'L0–L2'],
              ].map(([tech, abbr, func, level], i) => (
                <tr key={i} style={{ borderBottom: '1px solid #1e1e20', backgroundColor: i % 2 === 0 ? 'transparent' : 'rgba(255,255,255,0.012)' }}>
                  <td style={{ padding: '7px 10px', color: '#e3e3e8' }}>{tech}</td>
                  <td style={{ padding: '7px 10px' }}><code style={{ color: '#F68B1F', fontFamily: 'monospace', fontSize: '11px', backgroundColor: '#1a1a1c', padding: '1px 5px', borderRadius: '3px', border: '1px solid #303033' }}>{abbr}</code></td>
                  <td style={{ padding: '7px 10px', color: '#adadb8' }}>{func}</td>
                  <td style={{ padding: '7px 10px', color: '#79c0ff', fontFamily: 'monospace', fontSize: '11px' }}>{level}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>

        {/* Communication Protocols */}
        {sectionTitle('Industrial Communication Protocols')}
        <p style={{ fontSize: '14px', color: '#adadb8', lineHeight: '1.75', marginBottom: '1rem' }}>
          Fieldbus and industrial Ethernet protocols define how automation devices exchange data. Selection depends on bandwidth, real-time requirements, topology, and vendor ecosystem. The industry is converging on Gigabit Ethernet-based solutions with time-sensitive networking (TSN).
        </p>
        <div style={{ overflowX: 'auto', marginBottom: '1.5rem' }}>
          <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: '12px' }}>
            <thead>
              <tr style={{ borderBottom: '1px solid #303033' }}>
                {['Protocol', 'Medium', 'Max Speed', 'Topology', 'Ecosystem', 'Use Case'].map(h => (
                  <th key={h} style={{ padding: '7px 10px', textAlign: 'left', color: '#88888f', fontSize: '10px', fontWeight: '700', textTransform: 'uppercase', letterSpacing: '0.05em', whiteSpace: 'nowrap' }}>{h}</th>
                ))}
              </tr>
            </thead>
            <tbody>
              {[
                ['Modbus RTU',    'RS-485',   '115 kbps', 'Master/Slave', 'Universal',    'Legacy field devices'],
                ['Modbus TCP/IP', 'Ethernet', '100 Mbps', 'Star / Mesh',  'Universal',    'Modern PLC I/O'],
                ['PROFIBUS DP',   'RS-485',   '12 Mbps',  'Bus',          'Siemens',      'Drive & sensor I/O'],
                ['PROFINET IRT',  'Ethernet', '1 Gbps',   'Star / Ring',  'Siemens',      'Motion control, IIoT'],
                ['EtherNet/IP',   'Ethernet', '1 Gbps',   'Star / Mesh',  'Rockwell AB',  'Allen-Bradley PLCs'],
                ['DeviceNet',     'CAN bus',  '500 kbps', 'Bus',          'Rockwell AB',  'Legacy AB sensors'],
                ['CANopen',       'CAN bus',  '1 Mbps',   'Bus',          'CiA Consortium','Machine automation'],
                ['CC-Link IE',    'Ethernet', '1 Gbps',   'Ring',         'Mitsubishi',   'MELSEC systems'],
                ['OPC-UA',        'TCP/IP',   'Variable', 'Client-Server','Multi-vendor', 'IIoT semantic layer'],
                ['MQTT',          'TCP/IP',   'Variable', 'Pub/Sub',      'Cloud / IoT',  'Edge-to-cloud telemetry'],
              ].map(([proto, medium, speed, topo, eco, use], i) => (
                <tr key={i} style={{ borderBottom: '1px solid #1e1e20', backgroundColor: i % 2 === 0 ? 'transparent' : 'rgba(255,255,255,0.012)' }}>
                  <td style={{ padding: '7px 10px', color: '#e3e3e8', fontWeight: '600' }}>{proto}</td>
                  <td style={{ padding: '7px 10px', color: '#adadb8' }}>{medium}</td>
                  <td style={{ padding: '7px 10px' }}><code style={{ color: '#3fb950', fontFamily: 'monospace', fontSize: '11px' }}>{speed}</code></td>
                  <td style={{ padding: '7px 10px', color: '#adadb8' }}>{topo}</td>
                  <td style={{ padding: '7px 10px', color: '#F68B1F', fontSize: '11px' }}>{eco}</td>
                  <td style={{ padding: '7px 10px', color: '#88888f' }}>{use}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>

        {/* Safety Standards */}
        {sectionTitle('Functional Safety & Standards')}
        <p style={{ fontSize: '14px', color: '#adadb8', lineHeight: '1.75', marginBottom: '1rem' }}>
          Functional safety ensures that control systems correctly execute their safety functions. The <strong style={{ color: '#e3e3e8' }}>Safety Integrity Level (SIL)</strong> and <strong style={{ color: '#e3e3e8' }}>Performance Level (PL)</strong> frameworks quantify the required reliability of safety functions.
        </p>
        <div style={{ display: 'grid', gridTemplateColumns: isMobileView ? '1fr' : '1fr 1fr', gap: '0.625rem', marginBottom: '1.5rem' }}>
          {[
            { std: 'IEC 61508', scope: 'Functional Safety of E/E/PE Systems — the parent standard. Defines SIL 1–4.', accentColor: '#4a2472', borderColor: '#c084fc' },
            { std: 'IEC 62061', scope: 'Machinery Safety — SILCL approach for machine-integrated control systems.', accentColor: '#1b4b72', borderColor: '#60a5fa' },
            { std: 'ISO 13849',  scope: 'Safety-Related Control Systems — PLr a–e approach for machinery.', accentColor: '#1b5c22', borderColor: '#4ade80' },
            { std: 'IEC 61511',  scope: 'Safety Instrumented Systems — Process industry SIS design and validation.', accentColor: '#6e4814', borderColor: '#fb923c' },
          ].map(({ std, scope, accentColor, borderColor }) => (
            <div key={std} style={{ backgroundColor: '#1a1a1c', border: `1px solid ${accentColor}`, borderLeft: `3px solid ${borderColor}`, borderRadius: '7px', padding: '10px 14px' }}>
              <div style={{ fontSize: '12px', fontWeight: '700', color: '#e3e3e8', marginBottom: '5px', fontFamily: 'monospace' }}>{std}</div>
              <div style={{ fontSize: '11px', color: '#88888f', lineHeight: '1.5' }}>{scope}</div>
            </div>
          ))}
        </div>

        {/* Industry 4.0 */}
        {sectionTitle('Industry 4.0 & Digital Transformation')}
        <p style={{ fontSize: '14px', color: '#adadb8', lineHeight: '1.75', marginBottom: '1rem' }}>
          <strong style={{ color: '#e3e3e8' }}>Industry 4.0</strong> describes the convergence of cyber-physical systems, IIoT, cloud computing, AI/ML, and advanced robotics into a unified digital manufacturing paradigm. Core enabling technologies:
        </p>
        <div style={{ display: 'flex', flexDirection: 'column', gap: '0.5rem', marginBottom: '1.5rem' }}>
          {[
            { label: 'Digital Twin', desc: 'Virtual replicas of physical assets for simulation, commissioning, and real-time optimisation.' },
            { label: 'Edge Computing', desc: 'Real-time data processing at the source, reducing latency and cloud bandwidth costs.' },
            { label: 'OPC-UA over TSN', desc: 'Time-Sensitive Networking (IEEE 802.1) + OPC-UA provides deterministic, open IIoT communication.' },
            { label: 'AI & Predictive Maintenance', desc: 'ML models trained on vibration, temperature, and current signatures to predict failures before they occur.' },
            { label: 'Collaborative Robots (Cobots)', desc: 'Force-limited robots working safely alongside humans without fixed safety guarding.' },
            { label: 'Additive Manufacturing', desc: '3D printing integrated into automated lines for jigs, fixtures, and end-use parts.' },
          ].map(({ label, desc }) => (
            <div key={label} style={{ display: 'flex', gap: '10px', alignItems: 'flex-start', borderLeft: '2px solid #303033', paddingLeft: '12px' }}>
              <div>
                <span style={{ fontSize: '12px', fontWeight: '700', color: '#F68B1F' }}>{label}: </span>
                <span style={{ fontSize: '12px', color: '#adadb8' }}>{desc}</span>
              </div>
            </div>
          ))}
        </div>

        {/* Feedback */}
        <div style={{ marginTop: '4rem', paddingTop: '3rem', borderTop: '1px solid #1e1e20' }}>
          {!feedback ? (
            <div style={{ display: 'flex', alignItems: 'center', gap: '2rem' }}>
              <span style={{ fontSize: '14px', fontWeight: '500', color: '#88888f' }}>Was this page helpful?</span>
              <button onClick={() => setFeedback('yes')} style={{ color: '#F68B1F', fontSize: '14px', fontWeight: '600', background: 'none', border: 'none', cursor: 'pointer' }}>Yes</button>
              <button onClick={() => setFeedback('no')}  style={{ color: '#F68B1F', fontSize: '14px', fontWeight: '600', background: 'none', border: 'none', cursor: 'pointer' }}>No</button>
            </div>
          ) : (
            <p style={{ fontSize: '14px', color: '#e3e3e8', backgroundColor: '#1a1a1c', padding: '0.75rem 1.5rem', borderRadius: '6px', display: 'inline-block', border: '1px solid #303033' }}>
              {feedback === 'yes' ? '✨ Thanks! Glad this was useful.' : '🙏 Thank you — we will improve this page.'}
            </p>
          )}
          <div style={{ paddingBottom: '6rem' }}/>
        </div>
      </div>

      {/* ═══════════ RIGHT COLUMN ═══════════ */}
      <div style={{
        width: isMobileView ? '100%' : '42%',
        backgroundColor: 'rgba(0,0,0,0.02)',
        padding: isMobileView ? '1.5rem 1rem' : '3rem',
        borderLeft: isMobileView ? 'none' : '1px solid #1e1e20',
        borderTop: isMobileView ? '1px solid #1e1e20' : 'none',
        boxSizing: 'border-box',
      }}>
        <div style={{ position: isMobileView ? 'static' : 'sticky', top: '3rem', display: 'flex', flexDirection: 'column', gap: '1.25rem' }}>

          {/* Tab bar */}
          <div style={{ display: 'flex', gap: '4px', backgroundColor: '#1a1a1c', border: '1px solid #303033', borderRadius: '10px', padding: '4px' }}>
            {tabs.map(t => (
              <button key={t.id} onClick={() => setActiveTab(t.id)} style={{
                flex: 1, padding: '7px 0', fontSize: '12px', fontWeight: '600', borderRadius: '7px',
                border: 'none', cursor: 'pointer',
                backgroundColor: activeTab === t.id ? '#F68B1F' : 'transparent',
                color: activeTab === t.id ? '#000' : '#88888f',
                transition: 'all 0.15s',
              }}>
                {t.label}
              </button>
            ))}
          </div>

          {/* Diagram card */}
          <div style={{ backgroundColor: '#1a1a1c', border: '1px solid #303033', borderRadius: '10px', padding: '1.25rem' }}>
            <h3 style={{ fontSize: '10px', fontWeight: '700', textTransform: 'uppercase', letterSpacing: '0.08em', color: '#88888f', marginBottom: '1rem', margin: '0 0 1rem' }}>
              {activeTab === 'pyramid' ? 'ISA-95 Automation Pyramid'
                : activeTab === 'architecture' ? 'System Architecture Overview'
                : 'Protocol Speed Comparison'}
            </h3>
            {activeTab === 'pyramid'      && <AutomationPyramid />}
            {activeTab === 'architecture' && <SystemArchitecture />}
            {activeTab === 'protocols'    && <ProtocolChart />}
          </div>

          {/* Info tip card */}
          <div style={{ backgroundColor: '#1a1a1c', border: '1px solid #303033', borderLeft: '4px solid #F68B1F', borderRadius: '8px', padding: '1.25rem' }}>
            <h3 style={{ fontSize: '10px', fontWeight: '700', textTransform: 'uppercase', letterSpacing: '0.08em', color: '#88888f', marginBottom: '0.625rem', margin: '0 0 0.625rem' }}>
              {activeTab === 'pyramid' ? 'About the ISA-95 Model'
                : activeTab === 'architecture' ? 'OT / IT Convergence'
                : 'Protocol Selection Guide'}
            </h3>
            <p style={{ fontSize: '12px', color: '#88888f', lineHeight: '1.65', margin: 0 }}>
              {activeTab === 'pyramid'
                ? 'Lower levels have faster response requirements (≤1 ms at L0) while upper levels handle planning horizons of hours to weeks at L4. Each level communicates only with the levels directly above and below it.'
                : activeTab === 'architecture'
                ? 'OT/IT convergence uses OPC-UA and MQTT as the "lingua franca" for secure, semantic data exchange from sensor to cloud. The demilitarised zone (DMZ) between L2 and L3 is a critical security boundary.'
                : 'Choose Ethernet-based protocols (PROFINET, EtherNet/IP) for new installations. Use OPC-UA as the vertical integration layer. Legacy fieldbus (PROFIBUS, Modbus) remains prevalent in brownfield sites.'}
            </p>
          </div>

        </div>
      </div>
    </div>
  );
};

<IndAutPage />
