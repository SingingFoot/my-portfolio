---
sidebar_position: 4
title: Mintlify
hide_table_of_contents: true
---

import React, { useState } from 'react';

export const Sparkles = ({ size = 16 }) => (
  <svg xmlns="http://www.w3.org/2000/svg" width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
    <path d="m12 3 1.912 5.813a2 2 0 0 0 1.275 1.275L21 12l-5.813 1.912a2 2 0 0 0-1.275 1.275L12 21l-1.912-5.813a2 2 0 0 0-1.275-1.275L3 12l5.813-1.912a2 2 0 0 0 1.275-1.275L12 3Z"/>
    <path d="M5 3v4"/><path d="M19 17v4"/><path d="M3 5h4"/><path d="M17 19h4"/>
  </svg>
);

export const CopyIcon = ({ size = 13 }) => (
  <svg xmlns="http://www.w3.org/2000/svg" width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
);

export const CheckIcon = ({ size = 13 }) => (
  <svg xmlns="http://www.w3.org/2000/svg" width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
);

export const MintlifyPage = () => {
  const [activeLang, setActiveLang] = useState(0);
  const [copied, setCopied] = useState({});

  const [isMobileView, setIsMobileView] = React.useState(
    typeof window !== 'undefined' ? window.innerWidth < 768 : false
  );
  React.useEffect(() => {
    const handler = () => setIsMobileView(window.innerWidth < 768);
    window.addEventListener('resize', handler);
    return () => window.removeEventListener('resize', handler);
  }, []);

  const langs = [
    {
      label: 'JavaScript',
      raw: `function getUserById(userId) {
  return db.users.findOne({ id: userId });
}`,
      docstring: `/**
 * Retrieves a user record by its unique identifier.
 *
 * @param {string} userId - The unique ID of the user to retrieve.
 * @returns {Promise<User|null>} The matching user object,
 *   or null if no user with that ID exists.
 * @throws {DatabaseError} If the database query fails.
 *
 * @example
 * const user = await getUserById('usr_4f2a9b');
 * console.log(user.email); // 'alice@example.com'
 */
function getUserById(userId) {
  return db.users.findOne({ id: userId });
}`,
    },
    {
      label: 'Python',
      raw: `def get_user_by_id(user_id: str) -> dict | None:
    return db.users.find_one({"id": user_id})`,
      docstring: `def get_user_by_id(user_id: str) -> dict | None:
    """Retrieve a user record by its unique identifier.

    Args:
        user_id (str): The unique ID of the user to retrieve.

    Returns:
        dict | None: The matching user document as a dictionary,
            or None if no user with the given ID exists.

    Raises:
        DatabaseError: If the database query encounters an error.

    Example:
        >>> user = get_user_by_id("usr_4f2a9b")
        >>> print(user["email"])
        'alice@example.com'
    """
    return db.users.find_one({"id": user_id})`,
    },
    {
      label: 'TypeScript',
      raw: `async function getUserById(userId: string): Promise<User | null> {
  return db.users.findOne({ id: userId });
}`,
      docstring: `/**
 * Retrieves a user record by its unique identifier.
 *
 * @param userId - The unique ID of the user to retrieve.
 * @returns A promise that resolves to the matching {@link User}
 *   object, or \`null\` if no user with that ID exists.
 * @throws {@link DatabaseError} If the underlying query fails.
 *
 * @example
 * \`\`\`ts
 * const user = await getUserById('usr_4f2a9b');
 * if (user) console.log(user.email);
 * \`\`\`
 */
async function getUserById(userId: string): Promise<User | null> {
  return db.users.findOne({ id: userId });
}`,
    },
  ];

  const copyCode = async (text, key) => {
    try {
      await navigator.clipboard.writeText(text);
      setCopied((prev) => ({ ...prev, [key]: true }));
      setTimeout(() => setCopied((prev) => ({ ...prev, [key]: false })), 2000);
    } catch (e) {}
  };

  const sectionTitle = (text) => (
    <h2 style={{ fontSize: '20px', fontWeight: 'bold', color: 'var(--ifm-font-color-base)', marginBottom: '0.5rem', marginTop: '3rem', letterSpacing: '-0.02em', paddingBottom: '0.5rem', borderBottom: '1px solid #1e1e20' }}>{text}</h2>
  );

  const inlineCode = (t) => (
    <code style={{ backgroundColor: '#1e1e20', padding: '0.125rem 0.4rem', borderRadius: '0.25rem', fontFamily: 'monospace', color: '#e3e3e8', fontSize: '13px' }}>{t}</code>
  );

  const renderCodeBlock = (code, key) => (
    <div style={{ position: 'relative' }}>
      <div style={{ position: 'absolute', right: '12px', top: '12px', zIndex: 10 }}>
        <button
          onClick={() => copyCode(code, key)}
          style={{ background: '#1a1a1c', color: '#555', padding: '6px', borderRadius: '4px', border: '1px solid #303033', cursor: 'pointer', display: 'flex', alignItems: 'center' }}
          title="Copy"
        >
          {copied[key] ? <CheckIcon /> : <CopyIcon />}
        </button>
      </div>
      <pre style={{ margin: 0, padding: '1.25rem', paddingRight: '3rem', fontFamily: 'monospace', fontSize: '12.5px', backgroundColor: '#0d0d0f', color: '#e3e3e8', lineHeight: '1.65', overflowX: 'auto', border: '1px solid #303033', borderRadius: '10px', whiteSpace: 'pre-wrap', wordBreak: 'break-word' }}>
        <code>{code}</code>
      </pre>
    </div>
  );

  const l = langs[activeLang];

  return (
    <div style={{
      display: 'flex',
      flexDirection: isMobileView ? 'column' : 'row',
      minHeight: '100vh',
      backgroundColor: 'var(--ifm-background-color)',
      margin: isMobileView ? '0' : '-2rem',
      borderTop: '1px solid #1e1e20',
    }}>

      {/* ── LEFT COLUMN ── */}
      <div style={{ flex: '1 1 0%', padding: isMobileView ? '1.5rem 1rem' : '4rem', color: 'var(--ifm-font-color-base)', borderRight: isMobileView ? 'none' : '1px solid #1e1e20' }}>
        <div style={{ maxWidth: '760px' }}>

          {/* Header */}
          <div style={{ display: 'flex', alignItems: 'center', gap: '0.75rem', marginBottom: '1rem' }}>
            <span style={{ backgroundColor: 'rgba(124,58,237,0.12)', color: '#7C3AED', padding: '0.25rem 0.625rem', borderRadius: '0.375rem', fontFamily: 'monospace', fontSize: '12px', fontWeight: '700', letterSpacing: '0.05em', border: '1px solid rgba(124,58,237,0.35)' }}>Doc platform</span>
            <code style={{ fontSize: '13px', color: '#adadb8', fontFamily: 'monospace' }}>mintlify.com</code>
          </div>
          <h1 style={{ fontSize: '2rem', fontWeight: 'bold', color: 'var(--ifm-font-color-base)', marginBottom: '1.5rem', letterSpacing: '-0.025em' }}>Mintlify for Technical Writers</h1>

          <div style={{ display: 'flex', flexDirection: 'column', gap: '1.25rem', fontSize: '15px', lineHeight: '1.8', marginBottom: '2rem' }}>
            <p>
              Mintlify is a documentation platform built specifically for developer-facing content. It combines a modern publishing infrastructure with AI-assisted writing tools — most notably <strong style={{ color: '#e3e3e8' }}>Mintlify Writer</strong>, which generates docstrings and documentation comments directly from your code.
            </p>
            <p style={{ borderLeft: '2px solid #7C3AED', paddingLeft: '1.5rem', fontStyle: 'italic', color: '#adadb8' }}>
              Mintlify Writer (the VS Code extension) and Mintlify Docs (the documentation platform) are separate products. Both are covered here.
            </p>
          </div>

          {/* What it is */}
          {sectionTitle('What it is')}
          <p style={{ fontSize: '14px', color: '#adadb8', lineHeight: '1.8', marginBottom: '1rem' }}>
            <strong style={{ color: '#e3e3e8' }}>Mintlify Docs</strong> is a documentation site platform that renders MDX files into a polished, searchable documentation site — similar to Docusaurus but hosted, with built-in search, analytics, and an AI chat widget. Configuration is a single {inlineCode('mint.json')} file.
          </p>
          <p style={{ fontSize: '14px', color: '#adadb8', lineHeight: '1.8', marginBottom: '1rem' }}>
            <strong style={{ color: '#e3e3e8' }}>Mintlify Writer</strong> is a VS Code extension that reads a function signature and generates a complete documentation comment — JSDoc, Python docstrings, or TypeScript annotations — in one keyboard shortcut. It works offline and requires no API key.
          </p>

          {/* Use cases */}
          {sectionTitle('Use cases for technical writers')}
          <div style={{ display: 'flex', flexDirection: 'column', gap: '1.5rem', marginBottom: '1.5rem' }}>
            {[
              {
                n: '1',
                title: 'Generate code docstrings',
                body: 'Place the cursor on any function and press the Mintlify Writer shortcut (Cmd+. on macOS). It reads the function name, parameters, and return type and generates a complete documentation comment. Works with JavaScript, TypeScript, Python, Java, Go, Rust, and more.',
              },
              {
                n: '2',
                title: 'Auto-document API endpoints',
                body: 'Paste a REST endpoint handler into Mintlify Writer and generate documentation that covers the HTTP method, path parameters, query parameters, request body, response codes, and usage examples. Use the output as a baseline for your API reference.',
              },
              {
                n: '3',
                title: 'Maintain documentation alongside code',
                body: 'Because Mintlify Writer sits inside VS Code, documentation can be updated in the same commit as the code change. This dramatically reduces the lag between code and docs — the leading cause of documentation rot.',
              },
              {
                n: '4',
                title: 'Publish a developer documentation site',
                body: 'Mintlify Docs renders MDX files with built-in components for API endpoints, code tabs, callouts, and interactive examples. Connect your GitHub repo and Mintlify deploys automatically on every push.',
              },
              {
                n: '5',
                title: 'Add AI-powered search and chat',
                body: 'Mintlify Docs includes an AI chat widget powered by your own documentation content. Users can ask natural-language questions and get answers with direct links to the relevant pages — reducing support load without any additional configuration.',
              },
            ].map(({ n, title, body }) => (
              <div key={n} style={{ display: 'grid', gridTemplateColumns: '28px 1fr', gap: '1rem', alignItems: 'flex-start' }}>
                <div style={{ width: '28px', height: '28px', borderRadius: '50%', backgroundColor: '#1a1a1c', border: '1px solid #7C3AED', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '11px', fontWeight: '700', color: '#7C3AED', fontFamily: 'monospace', flexShrink: 0, marginTop: '2px' }}>{n}</div>
                <div>
                  <div style={{ fontSize: '14px', fontWeight: '700', color: '#e3e3e8', marginBottom: '0.375rem' }}>{title}</div>
                  <div style={{ fontSize: '13px', color: '#adadb8', lineHeight: '1.7' }}>{body}</div>
                </div>
              </div>
            ))}
          </div>

          {/* mint.json */}
          {sectionTitle('Mintlify Docs — key configuration')}
          <p style={{ fontSize: '14px', color: '#adadb8', lineHeight: '1.8', marginBottom: '1rem' }}>
            All site-level settings live in {inlineCode('mint.json')} at the repository root. This single file controls navigation, branding, analytics integrations, and the AI assistant.
          </p>
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            {[
              ['"name"', 'Site name displayed in the browser tab and header.'],
              ['"logo"', 'Paths to light and dark logo variants.'],
              ['"navigation"', 'Nested array of groups and pages that defines the sidebar structure.'],
              ['"colors"', 'Primary, light, and dark accent colour hex values.'],
              ['"api"', 'Base URL for API playground — enables live request testing inside the docs.'],
              ['"analytics"', 'Connect PostHog, Mixpanel, or other analytics providers.'],
            ].map(([key, desc]) => (
              <div key={key} style={{ display: 'grid', gridTemplateColumns: '140px 1fr', gap: '1rem', padding: '0.75rem 0', borderBottom: '1px solid #1e1e20', fontSize: '13px', alignItems: 'baseline' }}>
                <code style={{ color: '#7C3AED', fontFamily: 'monospace' }}>{key}</code>
                <span style={{ color: '#adadb8', lineHeight: '1.55' }}>{desc}</span>
              </div>
            ))}
          </div>

          {/* Best practices */}
          {sectionTitle('Best practices')}
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            {[
              ['Use as a baseline', 'AI-generated docstrings are starting points, not finished copy. Verify that parameter descriptions match actual runtime behaviour and add edge cases the model doesn\'t know about.'],
              ['Edit before committing', 'Review generated docstrings in the same PR as the code. Committing unreviewed auto-generated documentation is how inaccurate content accumulates.'],
              ['Pair with a style guide', 'Set consistent rules for docstring format across the codebase — single-line summary, then a blank line, then detail. Mintlify Writer respects the style of surrounding docstrings.'],
              ['Leverage the API playground', 'Mintlify Docs\' built-in API playground lets users run live requests from within the documentation. This dramatically reduces the need for Postman collections and reduces support questions.'],
            ].map(([title, body]) => (
              <div key={title} style={{ display: 'grid', gridTemplateColumns: '155px 1fr', gap: '1rem', padding: '0.875rem 0', borderBottom: '1px solid #1e1e20', alignItems: 'baseline', fontSize: '13px' }}>
                <span style={{ color: '#7C3AED', fontWeight: '600' }}>{title}</span>
                <span style={{ color: '#adadb8', lineHeight: '1.65' }}>{body}</span>
              </div>
            ))}
          </div>

          {/* Closing */}
          <div style={{ marginTop: '3rem', padding: '1.5rem', backgroundColor: '#1a1a1c', border: '1px solid #303033', borderRadius: '0.75rem', borderLeft: '4px solid #7C3AED' }}>
            <p style={{ fontSize: '14px', color: '#adadb8', lineHeight: '1.8', margin: 0 }}>
              AI is not replacing technical writers — it is redefining the role. Writers who learn to work with AI produce better documentation faster, with greater consistency and scalability. The question is no longer <em>whether</em> to use AI, but <em>how</em> to use it without sacrificing accuracy and craft.
            </p>
          </div>

          <div style={{ paddingBottom: '8rem' }}></div>
        </div>
      </div>

      {/* ── RIGHT COLUMN ── */}
      <div style={{ width: isMobileView ? '100%' : '42%', backgroundColor: 'rgba(0,0,0,0.02)', padding: isMobileView ? '1.5rem 1rem' : '3rem', borderLeft: isMobileView ? 'none' : '1px solid #1e1e20', borderTop: isMobileView ? '1px solid #1e1e20' : 'none' }}>
        <div style={{ position: isMobileView ? 'static' : 'sticky', top: '3rem', display: 'flex', flexDirection: 'column', gap: '1.5rem' }}>

          <div>
            <h3 style={{ color: 'var(--ifm-font-color-base)', fontSize: '11px', fontWeight: '700', textTransform: 'uppercase', letterSpacing: '0.08em', marginBottom: '0.875rem' }}>Docstring generator — before & after</h3>

            {/* Language tabs */}
            <div style={{ display: 'flex', gap: '0.375rem', marginBottom: '1rem' }}>
              {langs.map((lang, i) => (
                <button
                  key={lang.label}
                  onClick={() => setActiveLang(i)}
                  style={{
                    padding: '0.4rem 0.875rem',
                    borderRadius: '0.375rem',
                    fontSize: '12px',
                    fontWeight: '600',
                    border: `1px solid ${activeLang === i ? '#7C3AED' : '#303033'}`,
                    backgroundColor: activeLang === i ? 'rgba(124,58,237,0.12)' : '#1a1a1c',
                    color: activeLang === i ? '#7C3AED' : '#adadb8',
                    cursor: 'pointer',
                    transition: 'all 0.15s',
                  }}
                >{lang.label}</button>
              ))}
            </div>

            {/* Before */}
            <div style={{ fontSize: '11px', fontWeight: '700', textTransform: 'uppercase', letterSpacing: '0.07em', color: '#88888f', marginBottom: '0.375rem' }}>Input — undocumented function</div>
            {renderCodeBlock(l.raw, `raw-${activeLang}`)}

            <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem', margin: '0.875rem 0', color: '#7C3AED', fontSize: '12px', fontWeight: '600' }}>
              <svg xmlns="http://www.w3.org/2000/svg" width={14} height={14} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M12 5v14"/><path d="m19 12-7 7-7-7"/></svg>
              Mintlify Writer generates
            </div>

            {/* After */}
            <div style={{ fontSize: '11px', fontWeight: '700', textTransform: 'uppercase', letterSpacing: '0.07em', color: '#7C3AED', marginBottom: '0.375rem' }}>Output — with docstring</div>
            {renderCodeBlock(l.docstring, `doc-${activeLang}`)}
          </div>

          {/* Tip card */}
          <div style={{ backgroundColor: '#1a1a1c', borderRadius: '0.75rem', border: '1px solid #303033', padding: '1.5rem', display: 'flex', flexDirection: 'column', gap: '1rem', borderLeft: '4px solid #7C3AED', boxShadow: '0 20px 25px -5px rgba(0,0,0,0.1)' }}>
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
              <h3 style={{ fontSize: '11px', fontWeight: 'bold', textTransform: 'uppercase', letterSpacing: '0.1em', color: '#88888f', margin: 0 }}>VS Code shortcut</h3>
              <Sparkles size={18} />
            </div>
            <p style={{ fontSize: '12px', lineHeight: '1.65', color: '#88888f', margin: 0 }}>
              Place your cursor on the line above a function and press <code style={{ color: '#e3e3e8', fontFamily: 'monospace' }}>Cmd+.</code> (macOS) or <code style={{ color: '#e3e3e8', fontFamily: 'monospace' }}>Ctrl+.</code> (Windows/Linux) to trigger Mintlify Writer. The generated docstring appears above the function signature.
            </p>
          </div>

        </div>
      </div>
    </div>
  );
};

<MintlifyPage />
